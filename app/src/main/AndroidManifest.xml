<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.sfu.minefinder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Model.ConfigureActivity"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".Model.HelpScreenActivity" />
        <activity android:name=".Model.PlayActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".Model.MainMenuActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".Model.MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>